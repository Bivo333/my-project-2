# Moslistva — Изделия из лиственницы в Москве и МО

## 🌲 О проекте
Сайт по продаже пиломатериалов премиального качества (палубная доска, планкен, вагонка). 
Проект использует **Tailwind CSS** для стилизации и систему компонентов на **JavaScript** для модульности.

## 🛠 Быстрый старт

### Вариант 1: Разработка через CDN (текущий) ⚡
Просто откройте `index.html` или `ceny.html ` через **Live Server** в VS Code. Стили Tailwind подгружаются динамически через скрипт в `head`.

### Вариант 2: Продакшн (через CLI) 🚀
Когда проект будет готов к деплою, рекомендуется собрать оптимизированный CSS файл, чтобы избежать предупреждений в консоли:
1. **Установка зависимостей:** `npm install`
2. **Сборка:** `npm run build`
3. **Результат:** Чистый и минифицированный файл `style.css` будет готов к загрузке на хостинг.

## 📂 Структура проекта
```text
moslistva/
├── index.html          # Главная страница (SEO-заголовок, Hero-блок, Каталог)
├── ceny.html          # Прайс-лист (Интерактивная таблица изделий)
├── script.js           # Загрузка компонентов и логика активных ссылок меню
├── style.css           # Кастомные стили (эффекты навигации, анимации)
├── components/         # Общие части сайта
│   ├── header.html     # Шапка с навигацией
│   └── footer.html     # Подвал сайта
├── tailwind.config.js  # Конфигурация темы (цвета, шрифты)
└── package.json        # Настройки скриптов сборки и зависимости